# Next.js (apps/web) environment template

# Public URL to the FastAPI service.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Server-side override (optional — defaults to NEXT_PUBLIC_API_URL if unset).
AI_HELPER_API_URL=http://localhost:8000

# Legacy fallback for utilities that still read API_BASE_URL.
API_BASE_URL=http://localhost:8000
